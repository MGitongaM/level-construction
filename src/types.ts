export interface ImageProps {
  imageSrc: string;
  height: number;
  width: number;
  alt: string;
  classNames: string;
}
export interface VideoProps {
  videoSrc: string;
  height: number;
  width: number;
  autoplay:string;
  muted:boolean;
  playsinline:boolean;
  classNames: string;
}
